---
phase: 07-send-to-ai
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/dashboard/send-to-ai-button.tsx
  - src/components/dashboard/virtualized-opportunities-table.tsx
  - scripts/verify-send-to-ai.ts
  - src/components/ui/tooltip.tsx
  - src/app/layout.tsx
  - scripts/verify-a11y.ts
autonomous: true
requirements: [APPLY-01, APPLY-02]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Cada fila de Internships/Underclassmen (nunca Benefits) con row.url presente muestra un botón 'Send to AI' visible y operable por teclado, junto a StatusDropdown/NotesPopover, en ese orden"
    - "Un clic real en el botón copia al portapapeles el prompt real devuelto por generateApplyPrompt — o, si el portapapeles falla, lo muestra en un textarea seleccionable dentro del popover"
    - "Una fila cuya oportunidad no tiene url muestra el botón deshabilitado (nunca oculto de forma que rompa el layout), con la razón en su nombre accesible"
    - "Una fila en auto_fill_in_progress o ready_to_review muestra el label 'Reenviar prompt' en vez de 'Send to AI' — nunca cuando el status es submitted o cualquiera de los 6 manuales"
    - "Si generateApplyPrompt devuelve ok:false, el popover muestra el mensaje real de error con role=alert e icono, nunca un prompt vacío ni un crash"
    - "Tab desde NotesPopover alcanza el botón Send to AI; Enter lo abre; Escape lo cierra devolviendo foco al trigger — sin trampa de foco"
  artifacts:
    - "src/components/dashboard/send-to-ai-button.tsx — componente cliente, Popover controlado con 4 estados (copying/copied/clipboard_failed/config_error) + Tooltip con 3 estados de label"
    - "src/components/ui/tooltip.tsx — instalado vía `npx shadcn add tooltip`"
    - "src/app/layout.tsx — envuelto en TooltipProvider"
    - "src/components/dashboard/virtualized-opportunities-table.tsx — SendToAiButton wireado en la fila, columnas de HeaderRow redistribuidas (15/27/16/10/22/10)"
    - "scripts/verify-a11y.ts — extendido con el recorrido de teclado de SendToAiButton"
  key_links:
    - "SendToAiButton llama a generateApplyPrompt (Plan 07-01, src/app/actions/auto-apply.ts) pasando solo opportunityExternalId — nunca url/title/company/perfil del lado del cliente"
    - "SendToAiButton lee record?.status del mismo applicationsByExternalId map que StatusDropdown/NotesPopover ya reciben por fila en virtualized-opportunities-table.tsx — nunca una query propia"
    - "El botón nunca se renderiza dentro de BenefitsTable (src/app/page.tsx) — vive únicamente en VirtualizedOpportunitiesTable, que Benefits no usa, por eso 'nunca en Benefits' no necesita un chequeo de tab explícito (07-UI-SPEC.md nota arquitectónica)"
---

<objective>
Construir el botón "Send to AI" (`SendToAiButton`) tal como lo especifica `07-UI-SPEC.md` — trigger con Tooltip de 3 estados, Popover controlado con 4 estados (copiando/copiado/portapapeles-falló/error-de-config), fallback de textarea seleccionable — y wirearlo en cada fila de `VirtualizedOpportunitiesTable` (Internships/Underclassmen, nunca Benefits), con las columnas de la tabla redistribuidas y el recorrido de teclado extendido en `verify-a11y.ts`.

Purpose: Este plan es la mitad "UI/botón" de Phase 7 — consume el `generateApplyPrompt` ya construido y probado en Plan 07-01 (depends_on) y lo convierte en la interacción real que Juan usa con un clic (APPLY-01), cerrando el loop completo del milestone v1.1: click → prompt copiado → sesión externa → revisión de Juan → callback (Phase 6) → panel actualizado.
Output: `SendToAiButton`, `tooltip.tsx` instalado + `TooltipProvider` en el layout raíz, `VirtualizedOpportunitiesTable` wireado con columnas redistribuidas, `scripts/verify-a11y.ts` extendido.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-send-to-ai/07-CONTEXT.md
@.planning/phases/07-send-to-ai/07-UI-SPEC.md
@.planning/phases/07-send-to-ai/07-01-SUMMARY.md
@src/app/actions/auto-apply.ts
@src/lib/auto-apply-prompt.ts
@src/components/dashboard/notes-popover.tsx
@src/components/dashboard/status-dropdown.tsx
@src/components/dashboard/virtualized-opportunities-table.tsx
@src/lib/application-status.ts
@src/app/layout.tsx
@scripts/verify-a11y.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: SendToAiButton mínimo funcional + wireado en la fila, click real end-to-end</name>
  <files>src/components/dashboard/send-to-ai-button.tsx, src/components/dashboard/virtualized-opportunities-table.tsx, scripts/verify-send-to-ai.ts</files>
  <precondition>pnpm dev corriendo en http://localhost:3921 con AUTO_APPLY_CALLBACK_SECRET seteado, y con al menos una oportunidad real (Internships o Underclassmen) con url no vacía ya sincronizada en Postgres.</precondition>
  <action>
    Crear `src/components/dashboard/send-to-ai-button.tsx` ("use client"), forma mínima pero real (Task 2 la EXPANDE con Tooltip/Popover controlado completo — esta versión ya debe funcionar de punta a punta, no es un stub visual): props `{opportunityExternalId: string; url: string | null; status: ApplicationStatus; tabIndex?: number}` (mismo shape que `StatusDropdown`/`NotesPopover`). Calcula el label accesible: si `!url`, deshabilitado con `aria-label="Send to AI (sin link de aplicación)"`; si `status` es `"auto_fill_in_progress"` o `"ready_to_review"` (un `Set` de EXACTAMENTE esos 2 valores — nunca los 3 de `AUTO_APPLY_CALLBACK_STATUSES`, `submitted` cae al label por defecto), `aria-label="Reenviar prompt"`; si no, `aria-label="Send to AI"` (texto en inglés, deliberado per 07-CONTEXT.md, no traducir). Botón `variant="ghost" size="icon" className="size-8"` (mismo tamaño que `NotesPopover`) con ícono `Sparkles` de `lucide-react`, `disabled` cuando no hay `url` o mientras está pendiente. Al hacer click (no deshabilitado, no pendiente): `startTransition` llamando `generateApplyPrompt(opportunityExternalId)`; si `ok:true`, intenta `navigator.clipboard.writeText(result.prompt)` — si resuelve, marca un estado "copiado" (texto `aria-live="polite"` "Prompt copiado", mismo patrón de indicador de `NotesPopover`); si rechaza, guarda `result.prompt` en estado local y lo renderiza dentro de un `<textarea readOnly>` simple con el texto completo (fallback funcional real, aunque sin el pulido visual exacto de Popover/Textarea de shadcn — eso lo agrega Task 2); si `ok:false`, muestra `result.message` con `role="alert"`. No uses `Popover`/`Tooltip` de shadcn todavía en esta tarea — un `<div>` simple con el botón + el estado renderizado debajo alcanza para probar el flujo real de punta a punta.

    Wirea `SendToAiButton` en `virtualized-opportunities-table.tsx`: import, y agrégalo como tercer control dentro del mismo `<div className="flex items-center gap-1">` de la celda "Postulación", después de `NotesPopover`, pasando `opportunityExternalId={row.externalId}`, `url={row.url}`, `status={record?.status ?? "not_applied"}`, y el mismo `tabIndex` condicional (`focusedIndex === virtualRow.index ? undefined : -1`) que ya usan `StatusDropdown`/`NotesPopover`. No toques `HeaderRow` todavía (las columnas se redistribuyen en Task 3) — es esperable que el layout se vea apretado hasta entonces, esta tarea prueba funcionalidad, no pixel-perfect.

    Extiende `scripts/verify-send-to-ai.ts` (creado en 07-01-PLAN.md) con un modo Playwright (mismo patrón `chromium.launch()` que `scripts/verify-a11y.ts`/`capture-finish-screenshots.ts`): otorga permisos `clipboard-read`/`clipboard-write` al contexto del navegador, navega a `baseUrl`, ubica la primera fila con un botón "Send to AI" habilitado (`aria-label="Send to AI"`), haz click, espera el estado "copiado", y lee el portapapeles real (`page.evaluate(() => navigator.clipboard.readText())`). Assert que el texto contiene: la URL real de esa fila (`row.url`, extraída del DOM del link "Ver fuente" de la misma fila), la cadena `DATOS CONFIABLES`, y la cadena `curl -X POST` — prueba real de que el click en el navegador ejecuta la Server Action y copia el prompt real, no un mock.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm build && AUTO_APPLY_CALLBACK_SECRET=$AUTO_APPLY_CALLBACK_SECRET DATABASE_URL=$DATABASE_URL pnpm exec tsx scripts/verify-send-to-ai.ts http://localhost:3921 --click</automated>
  </verify>
  <done>Un click real en un navegador (Playwright, permisos de portapapeles concedidos) sobre una fila con url real ejecuta generateApplyPrompt y copia al portapapeles un texto que contiene la URL real de esa fila, el header de perfil, y el bloque curl; pnpm build compila limpio con SendToAiButton wireado en la tabla.</done>
</task>

<task type="auto">
  <name>Task 2: Tooltip + Popover controlado con los 4 estados de 07-UI-SPEC.md</name>
  <files>src/components/ui/tooltip.tsx, src/app/layout.tsx, src/components/dashboard/send-to-ai-button.tsx</files>
  <action>
    Instalar el componente Tooltip vía `npx shadcn add tooltip` (registro oficial de shadcn, preset `radix-nova` ya resuelve su estilo — confirmado vía Context7 `/shadcn-ui/ui`; no hay `tooltip.tsx` en `src/components/ui/` todavía per 07-UI-SPEC.md "Component Inventory"). Si el comando agrega una dependencia nueva a `package.json` (poco probable ya que `radix-ui@^1.6.7` es el paquete consolidado que ya cubre el primitive de Tooltip), anótalo en el SUMMARY — no es un install de un paquete de terceros nuevo, es el mismo registro shadcn ya usado para el resto de `components/ui/`.

    En `src/app/layout.tsx`, envolver `{children}` con `<TooltipProvider>` (import de `@/components/ui/tooltip`) dentro del `<body>`, sin tocar ninguna otra clase/estructura existente (Inter/IBM Plex Mono, `dark`, `bg-background`) — requerido para que cualquier `Tooltip` de la app funcione (confirmado vía Context7 `/shadcn-ui/ui`: "you must add the TooltipProvider to the root of your application").

    Reescribir `SendToAiButton` (creado en Task 1) para cumplir exactamente la "Interaction Pattern" de `07-UI-SPEC.md`, conservando la MISMA llamada a `generateApplyPrompt` + lógica de clipboard de Task 1 (no se reescribe la lógica de negocio, solo la capa visual):

    Trigger: envolver el `Button` (`variant="ghost" size="icon" className="size-8"`, ícono `Sparkles`) en `Tooltip`/`TooltipTrigger`/`TooltipContent`, con el texto del tooltip Y el `aria-label` sincronizados a los mismos 3 valores ya calculados en Task 1 ("Send to AI" / "Reenviar prompt" / "Send to AI (sin link de aplicación)").

    Popover CONTROLADO (`open`/`onOpenChange` en vez del modo no controlado de `NotesPopover`): al hacer click (botón no deshabilitado, no pendiente), `setOpen(true)` inmediatamente y arrancar la transition que llama `generateApplyPrompt`. `PopoverContent align="start"` con `className="w-80"` para los 3 estados de solo texto, `className="w-96"` únicamente para el estado con textarea. Renderizar EXACTAMENTE uno de estos 4 estados dentro del contenido (07-UI-SPEC.md "Click flow"):

    1. `copying` (transición en curso): `<p aria-live="polite" className="text-xs text-muted-foreground">Copiando…</p>`.
    2. `copied` (éxito, `writeText` resolvió): `<p aria-live="polite" className="text-xs text-muted-foreground">Prompt copiado</p>`; auto-cierra el Popover (`setOpen(false)`) a los 2000ms vía `setTimeout` con cleanup en un `useEffect` — sin textarea en este estado.
    3. `clipboard_failed` (Server Action ok, `writeText` rechazó): `<p aria-live="polite" className="text-xs text-muted-foreground">No se pudo copiar automáticamente. Selecciona el texto y cópialo manualmente.</p>` seguido de un `Textarea` (`@/components/ui/textarea`) `readOnly rows={12} className="font-mono text-xs"` con el `value` del prompt completo, que llama `.select()` sobre su `ref` en un `useEffect` al montar para dejarlo pre-seleccionado. El Popover se queda abierto — el cierre nativo de Radix (Escape/click-afuera) es la única salida, igual que `NotesPopover`.
    4. `config_error` (`generateApplyPrompt` devolvió `ok:false`): `<div role="alert" className="flex items-center gap-2 text-xs text-destructive"><TriangleAlert aria-hidden="true" className="size-3.5 shrink-0" /><span>{result.message}</span></div>` — sin `aria-live` (usa `role="alert"` en su lugar, ya que renderiza síncrono con la apertura del popover, no una actualización diferida); sin textarea (no se generó ningún prompt). El texto es `result.message` tal cual lo devuelve la Server Action (para el caso real de secret faltante será literalmente "Falta configurar AUTO_APPLY_CALLBACK_SECRET", per 07-CONTEXT.md).

    El botón queda `disabled={isPending}` durante `copying` (mismo criterio que `disabled={isPending}` de `StatusDropdown`), habilitado de nuevo en cualquiera de los 3 estados terminales.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm build</automated>
  </verify>
  <done>tooltip.tsx existe en src/components/ui/, TooltipProvider envuelve el layout raíz, y SendToAiButton renderiza los 4 estados del Popover controlado (copying/copied/clipboard_failed/config_error) y los 3 estados de tooltip/aria-label exactamente como 07-UI-SPEC.md los especifica, sin haber tocado la lógica de generateApplyPrompt/clipboard de Task 1; pnpm build compila limpio.</done>
</task>

<task type="auto">
  <name>Task 3: Columnas redistribuidas + recorrido de teclado en verify-a11y.ts + build final</name>
  <files>src/components/dashboard/virtualized-opportunities-table.tsx, scripts/verify-a11y.ts</files>
  <precondition>pnpm dev corriendo en http://localhost:3921.</precondition>
  <action>
    En `HeaderRow` (`virtualized-opportunities-table.tsx`), redistribuir los anchos fijos EXACTAMENTE per 07-UI-SPEC.md "Layout — Row Placement" (la tabla usa `table-fixed`, así que estos anchos son la ÚNICA fuente de verdad de las columnas, ver el comentario ya existente sobre por qué): Company `16% → 15%`, Title `28% → 27%`, Location `18% → 16%`, Status `10%` (sin cambio), Postulación `18% → 22%`, Link `10%` (sin cambio) — confirmar que suman 100% antes y después. Si al medir el resultado real renderizado (Tooltip/Popover ya montados) un punto de overflow distinto aparece, un ajuste pequeño dentro de esta misma lógica de redistribución (encoger Company/Location, crecer Postulación) está en alcance sin necesitar un nuevo UI-SPEC, per 07-UI-SPEC.md.

    Extender `scripts/verify-a11y.ts`: después del bloque existente que ya hace Tab → StatusDropdown → Enter → ArrowDown → Enter → Tab → NotesPopover → Enter → type → Escape (sección "9. Full in-row keyboard walkthrough"), agregar los pasos siguientes sobre la MISMA fila ya enfocada: `Tab` (NotesPopover trigger → SendToAiButton trigger) y assert que `document.activeElement`'s `aria-label` es uno de los 3 valores esperados ("Send to AI", "Reenviar prompt", o el texto que incluye "sin link de aplicación"); `Enter` para abrir el popover y assert que aparece contenido con `aria-live="polite"` o `role="alert"` (cualquiera de los 4 estados es válido, la fila real usada en el recorrido puede o no tener perfil/url); `Escape` para cerrar y assert (mismo patrón que el bloque de `NotesPopover` justo arriba) que el foco NO cae a `<body>` y el elemento enfocado es visible (`offsetParent !== null`) — sin trampa de foco, sin pérdida de foco.

    Correr el build final completo.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm build && pnpm exec tsx scripts/verify-a11y.ts http://localhost:3921</automated>
  </verify>
  <done>HeaderRow redistribuye sus 6 columnas sumando 100% con Postulación en 22%; scripts/verify-a11y.ts confirma que Tab alcanza SendToAiButton después de NotesPopover, Enter lo abre, Escape lo cierra sin trampa ni pérdida de foco; pnpm build compila limpio con la tabla completa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fila renderizada (cliente) → SendToAiButton → generateApplyPrompt | El componente solo envía `opportunityExternalId` a la Server Action (Plan 07-01) — nunca `url`/`title`/`company`/perfil, que se releen server-side |
| Fallback de portapapeles (`Textarea` visible en pantalla) | Muestra el prompt completo, secret incluido, en el DOM local — nunca se transmite a ningún otro destino |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Repudiation | Instrucción de "esperar el OK de Juan antes de enviar" (sección 6 del prompt, Plan 07-01) | medium | accept | La app no puede verificar técnicamente que la sesión externa respetó esa instrucción — se confía en el texto del prompt + revisión humana declarada, no en un mecanismo técnico de bloqueo; mismo límite ya aceptado en REQUIREMENTS.md "Out of Scope" ("Auto-submit sin revisión humana") |
| T-07-06 | Information Disclosure | Textarea de fallback de portapapeles muestra el prompt (con el secret embebido) visible en pantalla | high | accept | Mismo riesgo ya aceptado como T-07-02/T-06-07 — el fallback es estrictamente local (`readOnly`, nunca se envía a ningún endpoint nuevo ni se loguea), solo cambia la superficie de exposición de "portapapeles" a "pantalla local de Juan" |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit && pnpm build` limpio
- `scripts/verify-send-to-ai.ts http://localhost:3921 --click` confirma el click real de punta a punta (Task 1)
- `scripts/verify-a11y.ts http://localhost:3921` confirma el recorrido de teclado completo, incluyendo SendToAiButton (Task 3)
- Revisión manual: el botón nunca aparece en la tabla de Benefits (`BenefitsTable` en `src/app/page.tsx` no importa `SendToAiButton`)
</verification>

<success_criteria>
Cada oportunidad de Internships/Underclassmen muestra un botón "Send to AI" operable por teclado y mouse (criterio de éxito #1 de Phase 7 en ROADMAP.md — APPLY-01); un clic genera y copia el prompt real de Plan 07-01 con fallback visible si el portapapeles falla; el botón refleja el estado de sesión externa activa (Phase 5/6) sin escribir nada él mismo — cerrando el loop completo del milestone v1.1 (click → prompt copiado → sesión externa con Playwright MCP → revisión de Juan → callback de Phase 6 → panel actualizado).
</success_criteria>

<output>
Create `.planning/phases/07-send-to-ai/07-02-SUMMARY.md` when done
</output>
---
phase: 04-deploy
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: []
autonomous: false
requirements: [DEPLOY-03]

estimate:
  tokens: 40000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The production Postgres schema (both committed migrations) is applied to the real 'opportunities' tenant before any traffic hits it"
    - "DATABASE_URL, GITHUB_PAT, SYNC_TRIGGER_SECRET are set as Dokploy environment variables on the application, never committed to git"
    - "The GitHub PAT set is confirmed read-only/fine-grained with an expiration date"
  artifacts:
    - "Dokploy application.saveEnvironment call recorded (values never written to any plan/SUMMARY file)"
  key_links:
    - "Local drizzle-kit migrate -> production DB via an SSH tunnel to the VPS-internal shared-postgres"
    - "application.saveEnvironment env string -> the 3 required secrets"
---

<objective>
Apply the two committed Drizzle migrations to the real production `opportunities` Postgres tenant (only reachable from inside the VPS network), and set the three required secrets as Dokploy environment variables on the application created in 04-01.

Purpose: The app cannot boot correctly against an unmigrated schema, and Dokploy env vars are the only place these secrets may live (never in git, per DEPLOY-03 and PITFALLS.md Pitfall 6). Doing migrations here (before first deploy) avoids a chicken-and-egg problem where the freshly-deployed container has no CLI access to run its own migrations.
Output: Production DB schema matches `drizzle/0000_*.sql` + `drizzle/0001_*.sql`; Dokploy application has all 3 env vars set.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/juan/Documents/Codigo/Personal/opportunities/.planning/phases/04-deploy/04-CONTEXT.md
@/Users/juan/Documents/Codigo/Personal/opportunities/.planning/phases/04-deploy/04-01-SUMMARY.md
@/Users/juan/Documents/Codigo/Personal/hosting/infra/API-DEPLOY-GUIDE.md
@/Users/juan/Documents/Codigo/Personal/opportunities/drizzle.config.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Obtain real DATABASE_URL and confirm GITHUB_PAT scope/expiration</name>
  <precondition>SSH access to juan@116.203.79.125 via ~/.ssh/sapling_ed25519 is available; the 'opportunities' tenant on shared-postgres already exists (04-CONTEXT.md)</precondition>
  <action>Neither value can be safely guessed or re-derived — the DB password and the PAT are secrets only Juan holds or can regenerate.</action>
  <instructions>
    I need two things before I can migrate the production DB and set Dokploy env vars:
    1. The real `DATABASE_URL` for the `opportunities` tenant on `shared-postgres`. If you don't have it handy, tell me and I'll SSH in and re-run `bash ~/infra-db/new-tenant-postgres.sh opportunities` myself — it's idempotent and won't rotate the password if the tenant already exists, so it's safe for me to run and it will print the existing connection string.
    2. Confirmation that the GitHub PAT you generated earlier is still valid, fine-grained, read-only, and has an expiration date set. If it lapsed or you're not sure, generate a new fine-grained read-only PAT with an expiration and paste it here instead.
  </instructions>
  <verification>Once provided, I will use the DATABASE_URL to open an SSH tunnel and run the production migration (Task 2), and use the PAT to set the Dokploy env var (Task 3) — I will not echo either value back or write them into any plan/SUMMARY file.</verification>
  <resume-signal>Paste the DATABASE_URL (or tell me to fetch it via SSH) and confirm/paste the GitHub PAT</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply production migrations via SSH tunnel</name>
  <files>(none — SSH + local drizzle-kit CLI only)</files>
  <precondition>Task 1 complete: real DATABASE_URL known</precondition>
  <action>
    Open an SSH tunnel to reach the VPS-internal-only `shared-postgres` from the local machine: `ssh -i ~/.ssh/sapling_ed25519 -L 15432:shared-postgres:5432 -N juan@116.203.79.125 &` (background it, capture the PID to kill afterward). Then run `DATABASE_URL="postgresql://<user>:<pass>@localhost:15432/opportunities" pnpm db:migrate` locally — this uses the project's own `drizzle-kit migrate`, which both applies `drizzle/0000_*.sql`+`drizzle/0001_*.sql` and correctly records them in Drizzle's migrations journal table (unlike piping raw SQL via `psql`, which would apply the schema but leave the journal out of sync for future migrations). Kill the SSH tunnel process when done. Never write the real `DATABASE_URL` value into any file, plan, or SUMMARY — reference only that it was set correctly.
  </action>
  <verify>
    <automated>MISSING — no automated check can run without a live SSH tunnel + real credentials at plan-authoring time; the executor runs `pnpm db:migrate` against the tunneled DB and checks its exit code is 0</automated>
  </verify>
  <done>`pnpm db:migrate` reports both migrations applied (or already-applied, if a prior manual run happened) against the tunneled production DB with no errors.</done>
</task>

<task type="auto">
  <name>Task 3: Set the 3 required environment variables on the Dokploy application</name>
  <files>(none — Dokploy API call only)</files>
  <precondition>Task 1 complete: real DATABASE_URL and confirmed GITHUB_PAT available; DOKPLOY_TOKEN and DOKPLOY_URL set; applicationId known from 04-01-SUMMARY.md</precondition>
  <action>
    Call `application.saveEnvironment` with `{"applicationId":"<id from 04-01>","env":"DATABASE_URL=<real tenant URL>\nGITHUB_PAT=<real fine-grained read-only PAT>\nSYNC_TRIGGER_SECRET=<existing value from earlier phases>","buildArgs":"","buildSecrets":"","createEnvFile":true}` — `buildArgs` and `buildSecrets` are both required as empty strings even though unused, per API-DEPLOY-GUIDE.md (a 400 results otherwise). Do not print the full `env` string to any log/terminal output that gets persisted in a plan file or SUMMARY — only confirm success/failure of the call.
  </action>
  <verify>
    <automated>curl -sf -H "x-api-key: $DOKPLOY_TOKEN" "$DOKPLOY_URL/api/application.one?applicationId=<applicationId>" | grep -q '"env"'</automated>
  </verify>
  <done>`application.one` for the applicationId shows a non-empty `env` field (value itself not logged); the 3 keys DATABASE_URL, GITHUB_PAT, SYNC_TRIGGER_SECRET are confirmed present by key name only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local machine -> VPS-internal Postgres | SSH tunnel crosses into the private `dokploy-network`-only DB, bypassing its normal internal-only reachability |
| Dokploy dashboard/API -> plaintext env vars | Anyone with Dokploy project access can read all 3 secrets in plaintext |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-04 | Information Disclosure | GITHUB_PAT in Dokploy env vars | high | mitigate | PAT is fine-grained, read-only, scoped to public repo metadata, with an expiration date (confirmed in Task 1) per PITFALLS.md Pitfall 6/Security Mistakes |
| T-04-05 | Information Disclosure | DATABASE_URL transiting an SSH tunnel + local shell | medium | mitigate | Tunnel is process-local (`-N`, no shell exposure), killed immediately after migration; value never written to any file the executor creates |
| T-04-06 | Information Disclosure | Dokploy env vars stored in plaintext, visible to anyone with dashboard access | medium | accept | Single-user project, only Juan has Dokploy access; documented as accepted per PITFALLS.md Pitfall 6 (no team-access risk here) |
| T-04-07 | Elevation of Privilege | Overscoped/non-expiring GitHub PAT if the wrong token gets used | high | mitigate | Task 1 explicitly blocks on confirming fine-grained + read-only + expiring before Task 2 sets it |

</threat_model>

<verification>
Production schema matches committed migrations (drizzle-kit migrate succeeds against the tunneled DB). Dokploy application has all 3 env var keys set, values unlogged.
</verification>

<success_criteria>
Production DB is migration-current and the Dokploy application has DATABASE_URL, GITHUB_PAT, and SYNC_TRIGGER_SECRET set, ready to deploy.
</success_criteria>

<output>
Create `.planning/phases/04-deploy/04-02-SUMMARY.md` when done — confirm env var keys were set (never the values) and that migrations succeeded.
</output>

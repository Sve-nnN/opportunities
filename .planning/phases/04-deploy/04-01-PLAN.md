---
phase: 04-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [DEPLOY-01]

estimate:
  tokens: 35000
  raw_tokens: 25000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Docker image builds locally and serves the app with static assets present (no 404s on /_next/static/*)"
    - "The repo is pushed to a GitHub remote Dokploy can pull from"
    - "A Dokploy project and application exist, wired to the pushed repo, with buildType=dockerfile"
  artifacts:
    - "GitHub remote origin on the local repo, pushed to master/main"
    - "Dokploy project (opportunities) + application record with a known applicationId"
  key_links:
    - "application.saveGitProvider customGitUrl -> the pushed GitHub repo URL"
    - "application.saveBuildType dockerfile:'Dockerfile', dockerContextPath:'.'"
---

<objective>
Verify the existing multi-stage Dockerfile actually produces a working standalone image (static assets included), get the app's source onto a GitHub remote Dokploy can pull from, and create the Dokploy project + application shell configured to build that repo with the Dockerfile.

Purpose: Everything downstream (env vars, deploy, domain) needs a live `applicationId` wired to a real, buildable source. This plan produces that.
Output: A local Docker image proven to serve correctly, a public GitHub repo with the code pushed, and a Dokploy application configured for buildType=dockerfile pointing at it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/juan/Documents/Codigo/Personal/opportunities/.planning/PROJECT.md
@/Users/juan/Documents/Codigo/Personal/opportunities/.planning/phases/04-deploy/04-CONTEXT.md
@/Users/juan/Documents/Codigo/Personal/hosting/infra/API-DEPLOY-GUIDE.md
@/Users/juan/Documents/Codigo/Personal/hosting/infra/apps/RUNBOOK-payload-node.md
@/Users/juan/Documents/Codigo/Personal/opportunities/Dockerfile
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Build and run the production Docker image locally, confirm static assets serve</name>
  <files>Dockerfile</files>
  <action>
    Run `docker build -t opportunities:local .` from the repo root. The build needs network access for `next/font/google` at build time (per 04-CONTEXT.md decision — do not add `--network none`). If the build fails, diagnose and fix the Dockerfile/`next.config.ts`/`package.json` directly (do not skip this — this is the single point where a broken standalone output would otherwise only surface after a live Dokploy deploy). Once built, run it locally with a throwaway env (`docker run --rm -p 3010:3000 -e DATABASE_URL=postgresql://fake -e GITHUB_PAT=fake -e SYNC_TRIGGER_SECRET=fake opportunities:local`) and `curl -sI http://localhost:3010/` plus `curl -sI http://localhost:3010/_next/static/` (any known static chunk path from the build output under `.next/static/`) to confirm the standalone server serves its own static assets (200, not 404) without a reverse proxy in front of it. The app may error on DB-dependent routes with a fake `DATABASE_URL` — that's expected and out of scope for this task; only the HTTP layer (process starts, listens on 3000, serves static assets) is being verified here.
  </action>
  <verify>
    <automated>docker build -t opportunities:local . && docker run -d --rm -p 3010:3000 -e DATABASE_URL=postgresql://fake -e GITHUB_PAT=fake -e SYNC_TRIGGER_SECRET=fake --name opportunities-local-check opportunities:local && sleep 3 && curl -sf -o /dev/null -w "%{http_code}\n" http://localhost:3010/_next/static/ ; docker stop opportunities-local-check</automated>
  </verify>
  <done>docker build succeeds; the running container's static asset path returns a non-404 response (200 for an existing chunk, or a directory-listing-style 403/404 is acceptable only if a specific real chunk filename from `.next/static/chunks/` returns 200 — confirm with an actual filename, not the bare directory).</done>
</task>

<task type="auto">
  <name>Task 2: Create a GitHub repo and push this codebase to it</name>
  <files>(none — git/gh operations, no source files changed)</files>
  <action>
    This repo has no git remote configured (confirmed: `git remote -v` returns nothing). Dokploy's simplest source path (per API-DEPLOY-GUIDE.md and RUNBOOK-payload-node.md) is a public repo git URL via `application.saveGitProvider` — no GitHub App install needed, unlike a private repo. Use `gh repo create Sve-nnN/opportunities --public --source=. --remote=origin --push` (gh is already authenticated as Sve-nnN per `gh auth status`) to create the repo under Juan's existing GitHub account and push the current `master` branch in one step. Before pushing, double check `git status` shows nothing sensitive staged and `.gitignore`/`.dockerignore` already exclude `.env*` (confirmed already correct). The repo is public — there is no proprietary business logic and no secrets are committed (all three secrets are Dokploy env vars only); note this choice is reversible (repo visibility can be flipped to private later, at the cost of then needing the Dokploy GitHub App installed on it).
  </action>
  <verify>
    <automated>git remote get-url origin && git ls-remote origin master</automated>
  </verify>
  <done>`git remote -v` shows an `origin` pointing at `github.com/Sve-nnN/opportunities`, and `master` is pushed and visible via `git ls-remote origin`.</done>
</task>

<task type="auto">
  <name>Task 3: Create the Dokploy project + application, wire the GitHub repo, set buildType=dockerfile</name>
  <files>(none — Dokploy API calls only)</files>
  <precondition>DOKPLOY_TOKEN and DOKPLOY_URL are set in the execution environment (per team-lead's stated setup); http reachability to the Dokploy API at that URL succeeds</precondition>
  <action>
    Call `project.create` with `{"name":"opportunities","description":"Opportunities Hub - internships/programs/benefits dashboard"}`, capture the returned `environmentId` (default `production` environment). Call `application.create` with `{"name":"opportunities","environmentId":"<envId>","description":"Next.js dashboard, Postgres tenant 'opportunities'"}`, capture `applicationId`. Wire the source with `application.saveGitProvider` `{"applicationId":"<id>","customGitUrl":"https://github.com/Sve-nnN/opportunities.git","customGitBranch":"master","customGitBuildPath":"/","watchPaths":[]}` (public repo path from Task 2 — no GitHub App needed). Set the build type with `application.saveBuildType` `{"applicationId":"<id>","buildType":"dockerfile","dockerfile":"Dockerfile","dockerContextPath":".","dockerBuildStage":null,"herokuVersion":"24","railpackVersion":"0.15.4","publishDirectory":null,"isStaticSpa":false}` — all fields required per API-DEPLOY-GUIDE.md, explicit `null`s for unused ones. Record `environmentId` and `applicationId` in the plan's SUMMARY.md for the next plan to reuse (do not guess or re-derive them).
  </action>
  <verify>
    <automated>curl -sf -H "x-api-key: $DOKPLOY_TOKEN" "$DOKPLOY_URL/api/application.one?applicationId=<applicationId>" | grep -q '"buildType":"dockerfile"'</automated>
  </verify>
  <done>`application.one` for the created `applicationId` returns `buildType:"dockerfile"`, `dockerfile:"Dockerfile"`, and a `customGitUrl` matching the pushed repo — confirmed via a live GET call, not assumed from the POST responses alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Public internet -> GitHub | Source code is now publicly readable (public repo) |
| Local machine -> Dokploy API | `DOKPLOY_TOKEN` grants full project/app management on the VPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-01 | Information Disclosure | Public GitHub repo `Sve-nnN/opportunities` | low | accept | No secrets or proprietary logic in source; `.env*` already gitignored and verified before push; all 3 real secrets live only in Dokploy env vars, never in git |
| T-04-02 | Tampering | `DOKPLOY_TOKEN` used from local shell | high | mitigate | Token only ever read from the execution environment variable, never echoed/logged in full, never written to a plan/SUMMARY file |
| T-04-03 | Spoofing | `application.saveGitProvider` pulling from a git URL | low | accept | HTTPS clone from github.com, no custom/self-hosted git server involved |

</threat_model>

<verification>
Local Docker image builds and serves static assets. GitHub repo exists and is pushed. Dokploy `application.one` confirms buildType=dockerfile wired to that repo.
</verification>

<success_criteria>
A live Dokploy `applicationId` exists, configured to build the pushed GitHub repo's Dockerfile — ready for env vars and deploy in the next plan.
</success_criteria>

<output>
Create `.planning/phases/04-deploy/04-01-SUMMARY.md` when done, including the exact `environmentId` and `applicationId` values for reuse by 04-02.
</output>

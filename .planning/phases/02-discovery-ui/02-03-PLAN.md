---
phase: 02-discovery-ui
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/db/queries/sync-log.ts
  - src/app/page.tsx
  - src/components/dashboard/freshness-badge.tsx
  - src/components/dashboard/stale-sync-banner.tsx
autonomous: true
requirements: [DISC-04]

estimate:
  tokens: 60000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Cada tab muestra hace cuánto se sincronizó exitosamente su fuente"
    - "Si una fuente lleva más de 6h sin sync exitoso, o su último sync_log marca success=false, Juan ve un banner de advertencia explícito"
    - "La tabla es completamente navegable por teclado, con foco siempre visible y contraste AA"
    - "El build queda auditado y documentado por el flujo de finish de Impeccable (DESIGN.md refleja el mundo construido)"
  artifacts:
    - src/db/queries/sync-log.ts
    - src/components/dashboard/freshness-badge.tsx
    - src/components/dashboard/stale-sync-banner.tsx
    - .impeccable/review/desktop.png
    - .impeccable/review/mobile.png
    - DESIGN.md
  key_links:
    - "freshness-badge.tsx y stale-sync-banner.tsx leen getLatestSyncPerSource() (src/db/queries/sync-log.ts), no un timestamp hardcodeado ni derivado del cliente"
    - "El flujo de finish (detect + screenshots + finish-reviewer + documenter) audita exactamente el page.tsx que Plan 1/2 construyeron, no una versión hipotética"
---

<objective>
Cerrar Phase 2: mostrar la frescura real del último sync por fuente (badge + banner de advertencia si stale/failed), hacer una pasada de accesibilidad completa sobre el dashboard ya funcional (teclado, foco, contraste, semántica), y correr el flujo de finish de Impeccable (detector + screenshots + finish-reviewer + documenter) para que `DESIGN.md` quede escrito desde el mundo realmente construido.

Purpose: DISC-04 es el único requirement de Phase 2 que falta después de Plan 1/2; la accesibilidad es un requisito duro y no negociable (PROJECT.md/PRODUCT.md); el finish flow es la condición de "terminado" del contrato de dirección visual locked (`FINISH:` line en la surface brief).
Output: Badge de frescura + banner de stale-sync funcionando sobre datos reales de `sync_log`, dashboard accesible verificado, y DESIGN.md generado.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-discovery-ui/02-CONTEXT.md
@.impeccable/surfaces/src-app-page-tsx.md
@PRODUCT.md
@.planning/phases/02-discovery-ui/02-01-SUMMARY.md
@.planning/phases/02-discovery-ui/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task: Freshness badge + stale/failed-sync banner from sync_log</name>
  <files>src/db/queries/sync-log.ts, src/app/page.tsx, src/components/dashboard/freshness-badge.tsx, src/components/dashboard/stale-sync-banner.tsx</files>
  <behavior>
    - getLatestSyncPerSource() returns, for each of the 3 sources, the most recent sync_log row by startedAt (regardless of success), or null if that source has never synced
    - A source whose latest sync_log row has finishedAt more than 6 hours ago, OR whose latest row has success=false, is flagged as "stale" by a pure helper function (e.g. isSourceStale(latestRow, now))
    - A source synced 45 minutes ago with success=true is NOT flagged as stale
  </behavior>
  <action>
    Add `getLatestSyncPerSource()` to `src/db/queries/sync-log.ts`: one query per source (or a single query with `DISTINCT ON (source) ... ORDER BY source, started_at DESC`) returning the latest `sync_log` row per of the 3 known sources. Add a pure `isSourceStale(latestRow: SyncLogRow | null, now: Date): boolean` helper (testable without a DB: true if `latestRow` is null, `latestRow.success === false`, or `now - latestRow.finishedAt > 6 hours`). Build `freshness-badge.tsx`: renders "Actualizado hace {N}h" (or "hace {N}min" under 1h) per the active tab's source, computed from `finishedAt`, in the monospace/tabular-nums style per the direction contract, placed in the top bar's right slot (per FIRST VIEWPORT). Build `stale-sync-banner.tsx`: a visible, icon+text warning banner (never color-only) shown above the table when `isSourceStale()` is true for the active tab's source, with distinct copy for "sin sync reciente (>6h)" vs "el último sync falló" (read `errorMessage` from the row when `success=false` and surface it, so a real failure is diagnosable from the UI, not just implied). Wire both into `page.tsx`, fed by `getLatestSyncPerSource()`, scoped per active tab (each of the 3 tabs maps to exactly one `source` value already present in `syncLog`/`opportunities`/`benefits`).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm build</automated>
  </verify>
  <done>Freshness badge shows a real "hace Xh"/"hace Xmin" value matching the actual latest `sync_log.finished_at` for the active tab's source; manually inserting a `sync_log` row with `success=false` or a `started_at` >6h old for one source shows the stale/failed banner on that tab only, other tabs unaffected.</done>
</task>

<task type="auto">
  <name>Task: Accessibility pass — keyboard, focus, contrast, semantics</name>
  <files>src/app/page.tsx, src/app/globals.css</files>
  <action>
    Audit the full dashboard against A11Y.md (https://github.com/fecarrico/A11Y.md) and PRODUCT.md's Accessibility section: (1) confirm the table uses native `<table>`/`<thead>`/`<tbody>`/`<th scope="col">` semantics (already true from Plan 1 — verify, don't rebuild); (2) confirm every interactive element (tabs, search input, filter chips, sort if any, external links) is reachable via Tab/Shift+Tab in a logical order and operable via Enter/Space, with no keyboard trap; (3) add/verify a visible focus ring (the violet accent, `outline` or `ring`, never `outline: none` without a replacement) on every focusable element, meeting a minimum 3:1 contrast against its background per WCAG 2.2 focus-visible guidance; (4) run a contrast check on body text, muted/secondary text, and the violet accent against the near-black background, adjusting any pairing that fails WCAG AA (4.5:1 for normal text, 3:1 for large text/UI components) — document the exact hex values used and their computed ratios in the task's commit message or SUMMARY; (5) add an `aria-live="polite"` region announcing the current filtered result count (e.g. "134 resultados") so screen reader users get feedback when search/filter changes the table without a page reload; (6) confirm the stale-sync banner and status pills are never conveyed by color alone (already true from Plan 1/3 Task 1 — verify).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm build</automated>
  </verify>
  <done>Full keyboard walkthrough (Tab through tabs, search, chips, table rows/links) reaches every control with a visible focus ring and no trap; contrast ratios for body text, muted text, and the violet accent against `#0B0B0D` all meet WCAG AA, with the checked values recorded; filtered result count is announced via `aria-live`.</done>
</task>

<task type="auto">
  <name>Task: Impeccable finish flow — detect, screenshots, finish-reviewer, documenter</name>
  <files>.impeccable/review/desktop.png, .impeccable/review/mobile.png, DESIGN.md</files>
  <action>
    Follow reference/new-work.md step 7 (code-led path — no comp exists, this world's direction contract was recorded directly, per the design_authority note that this direction is locked, not re-decided): (1) run `~/.claude/skills/impeccable/scripts/impeccable detect --json` on the changed UI files (`src/app/page.tsx`, `src/app/layout.tsx`, `src/app/globals.css`, and every file under `src/components/dashboard/` and `src/components/ui/`), fix anything mechanical it flags (e.g. stock gradients, generic icon tiles, unauthored chrome) directly, and keep the remaining findings for the reviewer. (2) Capture `desktop.png` (1440px wide, full page) and `mobile.png` (390px wide) into `.impeccable/review/`, validating each capture shows real content (no blank/black regions, no half-loaded skeleton frozen mid-shimmer) before sending it onward. (3) Spawn the shipped `impeccable-finish-reviewer` subagent with: the original phase request (Discovery UI dashboard per DISC-01–04/BENE-01), the direction contract (read fresh from `.impeccable/surfaces/src-app-page-tsx.md` via `impeccable surface-brief read src/app/page.tsx`), the two screenshot paths, the `detect --json` findings, and one explicit line stating this is a code-led build with no approved comp (so it audits FIRST VIEWPORT + signature interaction promises in behavior, not against a comp diff). (4) Act on its disposition word exactly per new-work.md section 7 (`recapture` → recapture named evidence and re-review; `rebuild` → re-derive named regions wholesale and re-review, never a verdict pass; `fix` → apply the batch, rebuild, recapture, and send back for a verdict pass, up to 2 rounds; `ship` → proceed). (5) Once shipped, spawn the shipped `impeccable-documenter` subagent with the project root, `src/app/page.tsx` as the artifact path, the direction contract, `PRODUCT.md`, and the boundary to write `DESIGN.md` at the project root, so it records the durable visual system (palette, type, component language, spacing) from the built dashboard, not from intention.
  </action>
  <verify>
    <automated>test -f .impeccable/review/desktop.png && test -f .impeccable/review/mobile.png && test -f DESIGN.md</automated>
  </verify>
  <done>`.impeccable/review/desktop.png` and `mobile.png` exist and show the real built dashboard; the finish-reviewer's disposition is `ship` (or every `fix` round it required has been resolved and verdict-passed); `DESIGN.md` exists at the project root, written by the documenter from the built `page.tsx`/`layout.tsx`/`globals.css`, not copied from the direction contract verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sync_log.error_message (upstream fetch/parse errors) → rendered banner text | Error messages originate from ingestion code (Phase 1), not external user input, but are rendered as-is in the stale-sync banner |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Information Disclosure | `sync_log.error_message` surfaced in the stale-sync banner | low | accept | Error messages originate from this app's own ingestion code (Phase 1, not third-party user input) and are rendered as plain text via React's default escaping — no raw HTML injection path exists |
| T-02-06 | Elevation of Privilege | keyboard focus / accessibility affordances | low | accept | Focus-visible and ARIA-live additions are presentation-only; they expose no new data or write path beyond what Plan 1/2 already render |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` and `pnpm build` pass.
- Manual: freshness badge/banner reflect real `sync_log` state per tab; full keyboard walkthrough with visible focus; contrast checked and recorded.
- `.impeccable/review/desktop.png`, `mobile.png`, and `DESIGN.md` exist, with the finish-reviewer's disposition resolved to `ship`.
</verification>

<success_criteria>
- DISC-04 satisfied: every tab shows real last-sync freshness, with an explicit stale/failed warning when applicable.
- Dashboard passes a full keyboard + contrast accessibility walkthrough (A11Y.md / PRODUCT.md hard requirement).
- DESIGN.md exists, written from the shipped build via the Impeccable finish flow — Phase 2's FINISH condition is discharged.
</success_criteria>

<output>
Create `.planning/phases/02-discovery-ui/02-03-SUMMARY.md` when done
</output>

---
phase: 01-ingestion-foundation
plan: 2
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/ingestion/sources/summer-internships.ts
  - src/ingestion/sources/underclassmen.ts
  - src/ingestion/normalize.ts
  - src/ingestion/sync.ts
  - src/db/queries/upsert.ts
  - src/jobs/scheduled-sync.ts
  - src/app/api/sync/route.ts
autonomous: true
requirements: [ING-01, ING-02, ING-04, ING-05, ING-06]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Los datos de las 3 fuentes (Summer2027-Internships, underclassmen-opportunities, student-benefits) existen normalizados en Postgres tras un sync completo"
    - "Un sync programado actualiza las 3 fuentes periódicamente sin depender de fetch-por-request"
    - "Cada registro de opportunities tiene un external_id estable que sobrevive entre syncs, igual que benefits"
    - "Registros de opportunities que desaparecen de su fuente quedan is_active=false, nunca se borran"
    - "Una fila malformada en cualquiera de las 3 fuentes no aborta el sync completo de las otras fuentes"
  artifacts:
    - src/ingestion/sources/summer-internships.ts
    - src/ingestion/sources/underclassmen.ts
  key_links:
    - "sync.ts orquesta las 3 fuentes en paralelo con aislamiento de errores por fuente (una fuente que falla no bloquea a las otras)"
    - "jobs/scheduled-sync.ts y app/api/sync/route.ts ahora corren el sync completo de 3 fuentes, misma función runSync()"
    - "normalize.ts sigue siendo el único punto donde se decide la forma común de Opportunity, sin importar la fuente"
---

<objective>
Extend the proven ingestion architecture from Plan 1 to the two remaining sources — `SimplifyJobs/Summer2027-Internships` (via `listings.json`, per ING-01) and `Jose-Gael-Cruz-Lopez/underclassmen-opportunities` (via `remark`+`remark-gfm`, per ING-02) — completing full 3-source coverage of the phase goal.

Purpose: Plan 1 proved the architecture (auth, normalize, external_id, soft-delete, sync_log, dual trigger) on the simplest source. This plan applies that same proven shape to the two harder sources without re-deciding any architecture, per the tracer-first ordering.
Output: `sync.ts` orchestrates all 3 sources with per-source error isolation; `opportunities` table is fully populated from both GitHub sources; cron and manual trigger now run the complete 3-source sync.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-ingestion-foundation/01-CONTEXT.md
@.planning/phases/01-ingestion-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SimplifyJobs Summer2027-Internships parser via listings.json</name>
  <files>src/ingestion/sources/summer-internships.ts, src/ingestion/normalize.ts</files>
  <action>
    In `src/ingestion/sources/summer-internships.ts`, fetch `https://raw.githubusercontent.com/SimplifyJobs/Summer2027-Internships/dev/.github/scripts/listings.json` with the same `Authorization: Bearer ${process.env.GITHUB_PAT}` pattern from Plan 1 (never the rendered README table, per PITFALLS.md Pitfall 2). If the `dev` branch path 404s, fall back to `main` — verify the actual current branch/path against the live repo during implementation, since SimplifyJobs' branch naming has changed across seasons. Validate the response array with zod against the known `listings.json` field shape (`company_name`, `title`, `locations` (array), `date_posted`, `active` (boolean), `url`, plus any additional fields — treat unknown/extra fields as pass-through, not a validation failure, since community JSON evolves). Log and skip any entry that fails validation on required fields rather than aborting the whole fetch.

    In `src/ingestion/normalize.ts`, add `normalizeInternship(raw)` mapping to the `opportunities` table shape: `source = 'summer2027-internships'`, `title = raw.title`, `company = raw.company_name`, `location = raw.locations.join(', ')`, `category` derived from the listing's role category field if present else null, `role_type` similarly, `url = raw.url`, `is_active = raw.active`, `posted_at` parsed from `raw.date_posted`, `raw` = the original object (jsonb). Compute `external_id = sha1('summer2027-internships:' + raw.company_name + ':' + raw.title + ':' + raw.locations.join(','))` per the ARCHITECTURE.md external_id pattern (content-derived, not the row's `active` status, so a listing flipping open/closed keeps the same id and just changes `is_active`).
  </action>
  <verify>
    <automated>pnpm tsx -e "import('./src/ingestion/sources/summer-internships.ts').then(async m => { const rows = await m.fetchSummerInternships(); if (!rows.length) throw new Error('empty'); console.log(rows.length); })"</automated>
  </verify>
  <done>Fetching and parsing `listings.json` returns a non-empty array of validated rows; `normalizeInternship` produces a `company` and stable `external_id` for every parsed row without throwing.</done>
</task>

<task type="auto">
  <name>Task 2: underclassmen-opportunities parser via remark-gfm</name>
  <files>src/ingestion/sources/underclassmen.ts, src/ingestion/normalize.ts</files>
  <action>
    In `src/ingestion/sources/underclassmen.ts`, fetch the raw README of `Jose-Gael-Cruz-Lopez/underclassmen-opportunities` (default branch — verify against the live repo) with the same PAT-authenticated header pattern. Parse it with `unified().use(remarkParse).use(remarkGfm).parse(markdown)` into an mdast tree, walk the tree to find the GFM `table` node(s), and extract rows as arrays of cell text (strip any inline HTML tags defensively per PITFALLS.md Pitfall 2 — a cell containing `<img>` badges or `<br>` should degrade to plain text, not crash the walk). Determine the column-to-field mapping by inspecting the table's header row at runtime (do not hardcode a column index that assumes a fixed order) and add a sanity check: if the header row's column count doesn't match the expected known columns (program/opportunity name, description or category, link), log a warning and skip that table rather than throwing.

    In `src/ingestion/normalize.ts`, add `normalizeUnderclassmenRow(raw)` mapping the extracted cells to the `opportunities` table shape: `source = 'underclassmen-opportunities'`, `title` = the program/opportunity name column, `company` = null (or an org name if a distinct column exists), `category`/`role_type` = the description/eligibility column if present, `url` = extracted from the row's markdown link syntax `[text](url)`, `is_active = true` (this source has no explicit open/closed signal — confirm this against the live table and document it as a known limitation, not a bug), `raw` = the original cell array (jsonb). Compute `external_id = sha1('underclassmen-opportunities:' + title + ':' + url)`.
  </action>
  <verify>
    <automated>pnpm tsx -e "import('./src/ingestion/sources/underclassmen.ts').then(async m => { const rows = await m.fetchUnderclassmenOpportunities(); if (!rows.length) throw new Error('empty'); console.log(rows.length); })"</automated>
  </verify>
  <done>Fetching and parsing the underclassmen-opportunities README returns a non-empty array of rows extracted from its GFM table(s); a deliberately malformed test row (mismatched column count, HTML-in-cell) is logged and skipped rather than crashing the parser.</done>
</task>

<task type="auto">
  <name>Task 3: Orchestrate all 3 sources in sync.ts with per-source error isolation</name>
  <files>src/ingestion/sync.ts, src/db/queries/upsert.ts, src/jobs/scheduled-sync.ts, src/app/api/sync/route.ts</files>
  <action>
    Extend `runSync()` in `src/ingestion/sync.ts` to loop over all 3 sources (`student-benefits`, `summer2027-internships`, `underclassmen-opportunities`) running each source's fetch->normalize->upsert->soft-delete->sync_log cycle independently via `Promise.allSettled` (per PITFALLS.md Performance Traps: run in parallel with per-source error isolation — one source's failure must not block or fail the others). Each source still gets its own `sync_log` row via `startSyncLog`/`finishSyncLog` from Plan 1.

    In `src/db/queries/upsert.ts`, add `upsertOpportunities(rows, source)` mirroring `upsertBenefits`: upsert by `external_id` via `.onConflictDoUpdate`, then soft-delete any existing `opportunities` row with that `source` whose `external_id` was not in this run's row set (the soft-delete scope must be per-source, since a Summer2027 sync must never soft-delete underclassmen-opportunities rows and vice versa).

    Update `src/jobs/scheduled-sync.ts` and `src/app/api/sync/route.ts` — no code change needed if they already call `runSync()` generically, but verify the manual trigger response now reports per-source results (e.g. `{ ok: true, results: [{source, rowsUpserted, rowsSoftDeleted, success}, ...] }`) so a partial failure is visible in the API response, not just the DB.
  </action>
  <verify>
    <automated>pnpm tsx -e "import('./src/ingestion/sync.ts').then(m => m.runSync()).then(r => { if (!Array.isArray(r) || r.length !== 3) throw new Error('expected 3 source results'); console.log(JSON.stringify(r)); })" && psql "$DATABASE_URL" -c "SELECT source, count(*) FROM opportunities WHERE is_active = true GROUP BY source;" | grep -qE 'summer2027-internships|underclassmen-opportunities'</automated>
  </verify>
  <done>Running `runSync()` populates `opportunities` with active rows from both `summer2027-internships` and `underclassmen-opportunities`, each with a stable `external_id`, alongside the existing `benefits` rows; a `sync_log` row exists per source per run; simulating one source's fetch failure (e.g. temporarily pointing its URL at a 404) still lets the other two sources sync successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub raw content (2 additional sources) -> ingestion parsers | Same untrusted-external-data boundary as Plan 1, now applied to `listings.json` and a markdown-table README |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Tampering | `src/ingestion/sources/summer-internships.ts`, `underclassmen.ts` | medium | mitigate | zod validation on `listings.json`; column-count sanity check + HTML-stripping on the markdown table parse; malformed rows logged and skipped, never inserted raw |
| T-01-07 | Denial of Service | `sync.ts` orchestration (one slow/hanging source) | low | mitigate | `Promise.allSettled` isolates a hung/failing source so it cannot block the other two from completing and recording their own `sync_log` rows |

</threat_model>

<verification>
1. All 3 sources produce validated, normalized rows with stable `external_id` values.
2. `opportunities` table contains active rows from both `summer2027-internships` and `underclassmen-opportunities` after a full sync.
3. Soft-delete is correctly scoped per source (verified by checking that a Summer2027-only re-sync never flips `is_active` on an underclassmen row).
4. A simulated single-source failure (bad URL) does not prevent the other two sources from completing their sync and logging success.
5. `sync_log` has one row per source per sync run, all 3 phase requirements (ING-01, ING-02) traceable to populated `opportunities` rows.
</verification>

<success_criteria>
- All 3 sources from PROJECT.md are ingested, normalized, and cached in Postgres per the phase goal.
- The scheduled + manual sync path covers the full 3-source set with no duplicated ingestion logic.
- Every requirement in ING-01 through ING-06 is satisfied across Plan 1 + Plan 2 combined.
</success_criteria>

<output>
Create `.planning/phases/01-ingestion-foundation/01-02-SUMMARY.md` when done
</output>

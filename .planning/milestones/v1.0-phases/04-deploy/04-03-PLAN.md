---
phase: 04-deploy
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified: []
autonomous: true
requirements: [DEPLOY-01]

estimate:
  tokens: 30000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Dokploy application builds successfully from the pushed GitHub repo's Dockerfile"
    - "The deployed container stays up (not crash-looping) and is reachable over the internal dokploy-network"
  artifacts:
    - "A running container for the 'opportunities' application, confirmed via docker.getContainers"
  key_links:
    - "application.deploy -> deployment.readLogs (build) + application.readLogs (runtime) as the source of truth over applicationStatus alone"
---

<objective>
Trigger the actual Dokploy deploy for the application configured in 04-01/04-02, and confirm the container builds AND stays running (not just that the build reports success).

Purpose: `applicationStatus: done` only reflects build success (per API-DEPLOY-GUIDE.md) — a crash-looping container still reports `done`. This plan closes that gap before wiring the public domain in 04-04.
Output: A live, non-crash-looping container for the `opportunities` application on the VPS.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/juan/Documents/Codigo/Personal/opportunities/.planning/phases/04-deploy/04-01-SUMMARY.md
@/Users/juan/Documents/Codigo/Personal/opportunities/.planning/phases/04-deploy/04-02-SUMMARY.md
@/Users/juan/Documents/Codigo/Personal/hosting/infra/API-DEPLOY-GUIDE.md
@/Users/juan/Documents/Codigo/Personal/hosting/infra/apps/LESSONS-LEARNED.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trigger the deploy and poll build status</name>
  <precondition>DOKPLOY_TOKEN and DOKPLOY_URL set; applicationId from 04-01-SUMMARY.md; env vars already set per 04-02-SUMMARY.md</precondition>
  <action>
    Call `application.deploy` `{"applicationId":"<id>"}` (returns immediately, build is queued). Poll `application.one?applicationId=<id>` every ~15-20s for up to a few minutes until `applicationStatus` is `done` or `error`. If `error`, fetch the build log: `deployment.all?applicationId=<id>` to get the latest `deploymentId`, then `deployment.readLogs?deploymentId=<id>&tail=200` and diagnose — common causes per LESSONS-LEARNED.md are a lockfile/Node-version mismatch (not applicable here, this build uses `buildType:dockerfile`, not Nixpacks, so those specific lessons don't transfer directly, but a missing `.dockerignore` entry or a build-time network failure fetching `next/font/google` could still surface here). Fix and redeploy if needed; do not proceed to Task 2 until `applicationStatus` is `done`.
  </action>
  <verify>
    <automated>curl -sf -H "x-api-key: $DOKPLOY_TOKEN" "$DOKPLOY_URL/api/application.one?applicationId=<applicationId>" | grep -q '"applicationStatus":"done"'</automated>
  </verify>
  <done>`application.one` reports `applicationStatus:"done"` for the latest deploy.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the container actually stays up, not crash-looping</name>
  <precondition>Task 1's applicationStatus is done</precondition>
  <action>
    Fetch `application.one?applicationId=<id>` and note the `appName` field (Dokploy's random slug, not the friendly name — required to find the right containers). Call `docker.getContainers` and filter for that `appName`; confirm there is a single container in a running state, not repeated `Created`/`Exited` entries seconds apart (crash-loop signature per API-DEPLOY-GUIDE.md Debugging playbook). Then check `application.readLogs?applicationId=<id>&tail=200&since=all` (runtime logs — this is where a DB auth failure or `ERR_MODULE_NOT_FOUND`-style crash would actually show up, `deployment.readLogs` only covers the build) for a clean startup: expect a Next.js "Ready" log line and no unhandled DB connection errors (confirming Task 1 of 04-02's migration/env var work actually took effect). If crash-looping, diagnose from the runtime log (most likely candidates: `DATABASE_URL` malformed/unreachable, or a missing env var) and fix by re-running `application.saveEnvironment` with a correction, then `application.deploy` again.
  </action>
  <verify>
    <automated>curl -sf -H "x-api-key: $DOKPLOY_TOKEN" "$DOKPLOY_URL/api/application.readLogs?applicationId=<applicationId>&tail=50&since=all" | grep -qi "ready"</automated>
  </verify>
  <done>Runtime logs show a clean Next.js startup with no DB connection errors, and `docker.getContainers` shows one stable running container for the app's `appName` (no repeated Created/Exited entries).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Running container -> shared-postgres | Container reaches the DB over `dokploy-network` by service name at runtime (confirmed working pattern per LESSONS-LEARNED.md, unlike build-time) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-08 | Denial of Service | Unhandled crash-loop left undiagnosed, silently serving 502s | medium | mitigate | Task 2 explicitly requires confirming runtime stability via logs + container state, not just build status, before this plan is considered done |
| T-04-09 | Information Disclosure | Runtime logs could echo `DATABASE_URL`/`GITHUB_PAT` if the app logs its own env at startup | low | accept | App code does not log full env vars (confirmed: no such logging exists in `src/db/client.ts` or route handlers reviewed in Phase 1-3) |

</threat_model>

<verification>
`applicationStatus` is `done` and runtime logs + `docker.getContainers` confirm a stable, non-crash-looping container.
</verification>

<success_criteria>
The `opportunities` application is deployed and running stably on the VPS, ready for the domain/SSL step.
</success_criteria>

<output>
Create `.planning/phases/04-deploy/04-03-SUMMARY.md` when done.
</output>

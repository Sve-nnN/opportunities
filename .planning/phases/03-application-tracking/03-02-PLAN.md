---
phase: 03-application-tracking
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/dashboard/notes-popover.tsx
  - src/components/ui/popover.tsx
  - src/components/ui/textarea.tsx
  - src/app/actions/applications.ts
  - src/db/queries/applications.ts
  - src/app/page.tsx
  - src/components/dashboard/virtualized-opportunities-table.tsx
  - package.json
  - pnpm-lock.yaml
  - scripts/verify-a11y.ts
autonomous: false
requirements: [TRACK-02, TRACK-03]

estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Juan puede escribir una nota libre sobre cualquier fila de Internships/Underclassmen y la nota se autoguarda sin botón explícito, sobreviviendo un refresh o cambio de dispositivo"
    - "El ícono de nota distingue visualmente (relleno vs. outline) si la fila ya tiene una nota guardada, sin depender solo de color"
    - "La tabla de Internships (16,109+ filas) es navegable por teclado (Tab/flechas) en tiempo constante, sin la demora de 6-13s documentada en Phase 2, incluso con el dropdown de estado y el ícono de notas en cada fila"
    - "El foco no se pierde ni queda atrapado al navegar por teclado dentro de la lista virtualizada, incluyendo al abrir/cerrar el popover de notas"
  artifacts:
    - "src/components/dashboard/notes-popover.tsx — ícono relleno/outline + popover con textarea, autosave debounced ~500ms"
    - "src/components/dashboard/virtualized-opportunities-table.tsx — refactor de OpportunitiesTable sobre TanStack Virtual, mismas columnas (incluye StatusDropdown y NotesPopover de Plan 1/este plan)"
    - "scripts/verify-a11y.ts extendido — cobertura de teclado sobre status dropdown y notes popover"
  key_links:
    - "notes-popover.tsx invoca una Server Action de autosave con debounce en el cliente (useTransition + setTimeout, mismo patrón de debounce que search-bar.tsx de Phase 2) — nunca un botón Guardar"
    - "La virtualización mantiene roving tabindex o equivalente para que Tab/Shift+Tab y las flechas sigan alcanzando cada fila y sus controles (dropdown, ícono de notas) en orden, sin salto ni trampa de foco"
---

<objective>
Cerrar Application Tracking (Fase 3): agregar notas libres con autosave a cada oportunidad trackeada (TRACK-02), y resolver — como parte de esta misma fase, no después — el problema de performance/teclado de la tabla de Internships mediante virtualización de filas (TanStack Virtual), ya que el plan anterior sumó un control interactivo más por fila sobre las 16,109 filas existentes. Termina con una pasada de accesibilidad real (Playwright) sobre los nuevos controles.

Purpose: TRACK-02 completa el modelo de tracking; la virtualización es una decisión técnica ya bloqueada en 03-CONTEXT.md (no una mejora opcional) porque cada fila virtualizada ahora también renderiza un dropdown y un ícono de notas, empeorando el problema de 6-13s ya documentado en 02-03-SUMMARY.md si se deja sin resolver.
Output: Popover de notas con autosave, tabla de Internships/Underclassmen refactorizada sobre `@tanstack/react-virtual` con navegación por teclado funcional, y verificación de accesibilidad extendida.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-application-tracking/03-CONTEXT.md
@DESIGN.md
@src/app/page.tsx
@src/db/queries/applications.ts
@src/app/actions/applications.ts
@src/components/dashboard/status-dropdown.tsx
@scripts/verify-a11y.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy checkpoint — @tanstack/react-virtual + shadcn popover/textarea</name>
  <what-built>
    Dos instalaciones nuevas a auditar antes de escribir código: (1) `@tanstack/react-virtual` — paquete mantenido por TanStack (Tanner Linsley); es la decisión técnica ya locked en 03-CONTEXT.md, así que la auditoría solo confirma que el paquete real en el registro coincide con la expectativa (mantenedor, versión estable reciente, sin flags de deprecación). (2) `pnpm dlx shadcn@latest add popover textarea` — mismo criterio que 03-01 Task 1: si no introduce paquetes nuevos más allá del `radix-ui` meta-paquete ya auditado, no requiere re-auditoría.
  </what-built>
  <how-to-verify>
    1. Reviso `npm view @tanstack/react-virtual` (mantenedor, última versión, deprecación) y te muestro el resultado.
    2. Corro `pnpm dlx shadcn@latest add popover textarea` y te muestro el diff de `package.json`/`pnpm-lock.yaml`.
    3. Si aparece algún paquete nuevo no cubierto por lo anterior, te muestro su página en npmjs.com antes de continuar.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" para continuar, o indica qué paquete rechazar.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Notes popover with debounced autosave</name>
  <files>src/components/dashboard/notes-popover.tsx, src/components/ui/popover.tsx, src/components/ui/textarea.tsx, src/app/actions/applications.ts, src/db/queries/applications.ts, src/app/page.tsx</files>
  <behavior>
    - Test 1: `upsertApplicationNotes(externalId, "texto")` sobre un external_id sin fila previa en `applications` crea una fila con `notes="texto"` y `status="not_applied"` por defecto (no sobreescribe status si ya existía uno distinto)
    - Test 2: llamar `upsertApplicationNotes` dos veces seguidas sobre el mismo external_id dobla como update, no crea una segunda fila (mismo `onConflictDoUpdate` que `upsertApplicationStatus`)
    - Test 3: `getApplicationsByExternalIds` (ya existente de Plan 1) incluye el campo `notes` en el Map devuelto, no solo `status`/`isSaved`
  </behavior>
  <action>
    Extender `src/db/queries/applications.ts` con `upsertApplicationNotes(opportunityExternalId: string, notes: string)`, mismo patrón `onConflictDoUpdate` sobre el índice único ya creado en Plan 1, seteando solo el campo `notes` (no pisa `status` si la fila ya existía). Confirmar que `getApplicationsByExternalIds` ya expone `notes` en su tipo de retorno (extender el `select` si Plan 1 no lo incluyó). Agregar `updateApplicationNotes` a `src/app/actions/applications.ts` (`"use server"`, valida longitud máxima razonable ej. 2000 caracteres con Zod antes de escribir, luego `revalidatePath("/")`). Instalar `popover` y `textarea` de shadcn. Construir `NotesPopover` (client component): un botón-ícono (`StickyNote` relleno si `hasNotes`, `StickyNote` outline si no — lucide expone variantes fill via `fill="currentColor"`/stroke solamente, usar prop `className` con `fill-current` condicional) que abre un `&lt;Popover&gt;` con un `&lt;Textarea&gt;` controlado; on-change dispara un debounce de ~500ms (mismo patrón `setTimeout`+cleanup que `search-bar.tsx`, no una librería nueva) que llama `updateApplicationNotes` vía `useTransition`, mostrando un indicador textual discreto "Guardado"/"Guardando…" (nunca solo un ícono de check, per Never-Color-Alone Rule) dentro del popover. El popover debe cerrar con Escape y devolver el foco al botón que lo abrió (comportamiento nativo de Radix Popover, confirmar que no se sobreescribe). Wire en `page.tsx`/`OpportunitiesTable`: nueva columna o icono junto al `StatusDropdown` existente, pasando el `notes` actual del Map de Plan 1.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm build</automated>
  </verify>
  <done>Escribir en el textarea de una fila real persiste en `applications.notes` tras ~500ms sin botón Guardar (verificado con un script tsx contra Postgres real), el ícono cambia a relleno tras guardar una nota no vacía, y Escape cierra el popover devolviendo el foco al botón.</done>
</task>

<task type="tracer">
  <name>Task 3: Row virtualization (TanStack Virtual) with keyboard-navigable rows + a11y verification</name>
  <files>src/components/dashboard/virtualized-opportunities-table.tsx, src/app/page.tsx, scripts/verify-a11y.ts</files>
  <action>
    Refactorizar `OpportunitiesTable` (actualmente en `page.tsx`, renderiza las 16,109+ filas de Internships de una sola vez dentro del boundary cliente de `&lt;Tabs&gt;`) a un nuevo componente client `VirtualizedOpportunitiesTable` usando `useVirtualizer` de `@tanstack/react-virtual` sobre el contenedor con scroll ya existente (`&lt;div className="min-h-0 flex-1 overflow-auto"&gt;` en `page.tsx`) — mantener la semántica de tabla nativa (`&lt;table&gt;`/`&lt;thead&gt;` sticky/`&lt;tbody&gt;`, per DESIGN.md "Table: native `&lt;table&gt;`... no ARIA-role divs") envolviendo las filas virtualizadas dentro del `&lt;tbody&gt;` con posicionamiento absoluto/`transform: translateY` estándar del patrón TanStack Virtual, altura total del `&lt;tbody&gt;` fijada por `virtualizer.getTotalSize()`. Implementar roving tabindex sobre las filas: solo la fila actualmente enfocada (o la primera, si ninguna lo está) tiene `tabIndex={0}`, el resto `tabIndex={-1}`; flechas Arriba/Abajo mueven el foco a la fila anterior/siguiente (haciendo scroll-into-view si está fuera del viewport virtualizado vía `virtualizer.scrollToIndex`), Home/End saltan a la primera/última fila. El `StatusDropdown` y `NotesPopover` dentro de cada fila siguen siendo alcanzables por Tab normalmente una vez el usuario entra a esa fila (roving tabindex aplica a la fila como contenedor de entrada, no reemplaza la navegación interna de sus controles). Aplicar el mismo componente a Underclassmen (mismo componente compartido, prop `deemphasized` ya existente se preserva). Extender `scripts/verify-a11y.ts` (Playwright, patrón ya establecido en 02-03) con: (a) medir el tiempo hasta que el tab-switcher es alcanzable por teclado tras cargar Internships (debe bajar de los 6-13s documentados en 02-03-SUMMARY.md a un tiempo constante, ej. &lt;1s, ya que ahora solo se montan las filas visibles); (b) un recorrido de teclado real que entra a una fila, cambia el StatusDropdown con teclado (flechas + Enter), abre el NotesPopover con Enter/Espacio, escribe, cierra con Escape, y confirma que el foco nunca queda atrapado ni se pierde (aterriza en un elemento visible y tabulable después de cada paso).
  </action>
  <verify>
    <automated>DATABASE_URL=$DATABASE_URL pnpm exec tsx scripts/verify-a11y.ts</automated>
  </verify>
  <done>scripts/verify-a11y.ts confirma: tab-switcher alcanzable por teclado en tiempo constante (no 6-13s) con las 16,109+ filas de Internships, roving tabindex funcional (flechas mueven el foco entre filas, Home/End funcionan), y el recorrido completo dropdown+popover no produce ninguna trampa de foco.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client textarea → Server Action | El contenido de notas es texto libre del único usuario (Juan) pero se renderiza de vuelta en el DOM — debe tratarse como no confiable en cuanto a longitud/contenido antes de persistir |
| Client → TanStack Virtual render | Índices de fila calculados en cliente; deben mapear siempre a datos reales del array recibido en props, nunca a un índice fuera de rango |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | updateApplicationNotes Server Action | low | mitigate | Validar longitud máxima (Zod, ej. 2000 chars) antes de escribir en Postgres; texto plano en `text` column, sin interpretación HTML/markdown en el render (React escapa por defecto) |
| T-03-04 | Denial of Service | Notes autosave debounce | low | accept | Un solo usuario, debounce de 500ms ya limita la frecuencia de escritura; no hay rate-limit dedicado porque no hay superficie multi-usuario que abusarlo |
| T-03-SC | Tampering | pnpm install (@tanstack/react-virtual, shadcn popover/textarea) | high | mitigate | Package-legitimacy checkpoint (Task 1) — bloqueante para cualquier paquete nuevo no auditado |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit && pnpm build` limpio
- Script tsx confirma persistencia de notas contra Postgres real
- `scripts/verify-a11y.ts` extendido pasa: tiempo constante de alcanzabilidad por teclado + recorrido completo sin trampas de foco
</verification>

<success_criteria>
Juan puede agregar notas libres con autosave a cualquier oportunidad (TRACK-02), y la tabla de 16,109+ filas de Internships con dropdown de estado + ícono de notas por fila es navegable por teclado en tiempo constante, sin la demora de 6-13s heredada de Phase 2 — Fase 3 completa (TRACK-01 a TRACK-04).
</success_criteria>

<output>
Create `.planning/phases/03-application-tracking/03-02-SUMMARY.md` when done
</output>

---
phase: 04-deploy
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified: []
autonomous: false
requirements: [DEPLOY-02]

estimate:
  tokens: 25000
  raw_tokens: 16000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "opportunities.juan-tech.com resolves to the VPS and serves the app over HTTPS"
    - "Cloudflare SSL mode is Full (strict) with a valid Let's Encrypt cert on the origin, never a Cloudflare Origin CA cert"
    - "Juan has visually confirmed the live app renders real internship/benefits data, not just a 200 status"
  artifacts:
    - "Dokploy domain.create record for opportunities.juan-tech.com with certificateType=letsencrypt"
  key_links:
    - "Cloudflare DNS A record (Juan creates it) -> VPS public IP -> Dokploy domain config -> Traefik Let's Encrypt cert"
---

<objective>
Configure the custom domain + Let's Encrypt SSL on Dokploy for `opportunities.juan-tech.com`, and get a final human confirmation that the live site actually works end-to-end over HTTPS with real data.

Purpose: This is the last piece of DEPLOY-02, and the final acceptance check for the whole phase — it depends on a DNS record only Juan can create (per the team lead's brief, no Cloudflare API task is planned here), so it cannot be fully automated.
Output: `https://opportunities.juan-tech.com` is live, Full (strict) SSL, confirmed rendering real data.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/juan/Documents/Codigo/Personal/opportunities/.planning/phases/04-deploy/04-CONTEXT.md
@/Users/juan/Documents/Codigo/Personal/opportunities/.planning/phases/04-deploy/04-03-SUMMARY.md
@/Users/juan/Documents/Codigo/Personal/opportunities/.planning/research/PITFALLS.md
@/Users/juan/Documents/Codigo/Personal/hosting/infra/API-DEPLOY-GUIDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Confirm/create the Cloudflare DNS record (grey cloud, first issuance)</name>
  <precondition>04-03's application is deployed and running; the VPS public IP (116.203.79.125) is known</precondition>
  <action>The team lead's brief explicitly scopes Cloudflare DNS record creation to Juan doing it himself — no Cloudflare API task is planned here.</action>
  <instructions>
    Please create (or confirm you've already created) a DNS record for `opportunities.juan-tech.com` pointing at `116.203.79.125` in Cloudflare, set to **DNS-only (grey cloud)** for now — per PITFALLS.md Pitfall 5, a Cloudflare-proxied (orange cloud) record can break the initial Let's Encrypt HTTP-01 challenge. Once you confirm it's live and grey-cloud, I'll configure the Dokploy domain + trigger cert issuance (Task 2).
  </instructions>
  <verification>I'll run `dig opportunities.juan-tech.com` to confirm it resolves to 116.203.79.125 before proceeding to Task 2.</verification>
  <resume-signal>Type "done" once the DNS record is live and set to DNS-only (grey cloud)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Configure the Dokploy domain with Let's Encrypt and confirm cert issuance</name>
  <files>(none — Dokploy API call only)</files>
  <precondition>Task 1 confirmed: DNS record resolves to the VPS IP, grey-cloud</precondition>
  <action>
    Call `domain.create` `{"host":"opportunities.juan-tech.com","path":"/","port":3000,"https":true,"applicationId":"<id from 04-01>","certificateType":"letsencrypt","domainType":"application","stripPath":false}`. Wait for the certificate to issue (Traefik's ACME HTTP-01 challenge, typically under a minute once DNS resolves) and confirm with `curl -vI https://opportunities.juan-tech.com` showing a valid TLS handshake, no self-signed/cert warning.
  </action>
  <verify>
    <automated>curl -sf -o /dev/null -w "%{http_code}\n" https://opportunities.juan-tech.com/ | grep -qE "200|30[0-9]"</automated>
  </verify>
  <done>`domain.create` succeeds and `curl -vI https://opportunities.juan-tech.com` shows a valid Let's Encrypt cert with no warning.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Switch Cloudflare back to proxied (Full-strict) and confirm live data</name>
  <precondition>Task 2's Let's Encrypt cert issued successfully</precondition>
  <action>The cert is live on the origin; switching Cloudflare's proxy mode and doing a final visual/data check both require Juan directly (no API call substitutes for either per the team lead's brief and for genuine visual confirmation).</action>
  <instructions>
    The Let's Encrypt cert is issued. Please: (1) switch the `opportunities.juan-tech.com` Cloudflare record back to proxied (orange cloud) with SSL mode set to **Full (strict)** — never Cloudflare Origin CA certs, per the locked decision in 04-CONTEXT.md — and (2) open `https://opportunities.juan-tech.com` in your browser and confirm: valid HTTPS with no warning, the Internships/Underclassmen/Benefits tabs show real data (not empty states), and the "last synced" indicator shows a recent real timestamp (not a placeholder). If any tab is empty, I can trigger a manual sync via `curl -X POST -H "Authorization: Bearer $SYNC_TRIGGER_SECRET" https://opportunities.juan-tech.com/api/sync` — let me know and I'll run it.
  </instructions>
  <verification>I'll independently run `curl -s -o /dev/null -w "%{http_code}\n" https://opportunities.juan-tech.com/` to confirm 200 through the Cloudflare proxy once you've switched it back.</verification>
  <resume-signal>Type "approved" once HTTPS and real data are confirmed, or describe any issue</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Public internet -> Cloudflare -> Traefik origin | Standard proxied-domain boundary; SSL mode mismatch here is the most likely single point of failure for this whole phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-10 | Tampering | Cloudflare SSL mode mismatch (Flexible vs Full-strict) | high | mitigate | Explicitly locked to Full (strict) + Let's Encrypt per 04-CONTEXT.md; Task 1 verifies with a real curl TLS handshake, not just "domain saved" |
| T-04-SC | Tampering | N/A — no new package installs in this plan | n/a | n/a | Package legitimacy gate not applicable; no npm/pip/cargo installs in this phase |

</threat_model>

<verification>
`curl -vI https://opportunities.juan-tech.com` shows a valid Let's Encrypt cert; Juan confirms real data renders in the browser.
</verification>

<success_criteria>
All 3 Phase 4 success criteria are met: app runs as a Dokploy-managed Docker container, is reachable on `opportunities.juan-tech.com` with working HTTPS (Full-strict + Let's Encrypt), and secrets are Dokploy env vars with the PAT read-only and expiring.
</success_criteria>

<output>
Create `.planning/phases/04-deploy/04-04-SUMMARY.md` when done.
</output>

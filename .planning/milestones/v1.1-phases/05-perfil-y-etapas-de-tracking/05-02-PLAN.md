---
phase: 05-perfil-y-etapas-de-tracking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/application-status.ts
  - src/app/actions/applications.ts
  - src/components/dashboard/status-dropdown.tsx
  - scripts/verify-status-extension.ts
autonomous: true
requirements: [TRACK-05, TRACK-06]

estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "applications.status acepta los 3 valores nuevos (auto_fill_in_progress, ready_to_review, submitted) sin ninguna migración de DB — la columna sigue siendo text libre"
    - "El StatusDropdown de una postulación con uno de los 3 estados nuevos lo muestra con su propio ícono, texto y fondo violeta-tint — nunca solo color"
    - "Los 3 estados nuevos NUNCA aparecen como opción seleccionable dentro del SelectContent del dropdown — solo los 6 estados manuales existentes siguen siendo elegibles a mano"
    - "Si un cliente modificado intenta escribir uno de los 3 estados nuevos vía el Server Action manual (updateApplicationStatus), la escritura es rechazada — defensa en profundidad más allá de la UI"
  artifacts:
    - "src/lib/application-status.ts — APPLICATION_STATUSES (9 valores) + MANUALLY_SELECTABLE_STATUSES (los 6 originales, export nuevo)"
    - "src/app/actions/applications.ts — statusSchema restringido a MANUALLY_SELECTABLE_STATUSES"
    - "src/components/dashboard/status-dropdown.tsx — STATUS_META con 9 entradas, SelectContent iterando solo MANUALLY_SELECTABLE_STATUSES, fondo violeta-tint condicional en el trigger"
  key_links:
    - "STATUS_META sigue tipado Record<ApplicationStatus, {...}> (9 claves) — TypeScript fuerza el build a fallar si falta una entrada, mismo guardrail ya documentado en research/ARCHITECTURE.md"
    - "SelectValue sigue leyendo STATUS_META[status] para cualquiera de los 9 valores (desacoplado de SelectContent), por lo que mostrar un estado auto ya seteado en DB no requiere ningún cambio estructural"
---

<objective>
Extender las 6 etapas existentes de tracking con 3 etapas intermedias de solo-lectura (`auto_fill_in_progress`, `ready_to_review`, `submitted`) que reflejan una sesión de auto-apply a medias: la columna `applications.status` las acepta sin migración (sigue siendo `text` libre), y el `StatusDropdown` las muestra con ícono/texto/fondo propios pero nunca las ofrece como opción seleccionable a mano.

Purpose: TRACK-05/TRACK-06 son la mitad "Etapas de tracking" de Phase 5 — Phase 6 (callback API, fuera de este alcance) será el único escritor real de estos 3 valores; esta fase solo prepara la columna, el tipo, y la UI para que, cuando ese callback exista, el dashboard ya sepa mostrarlos correctamente sin que Juan pueda dispararlos por accidente desde el dropdown manual.
Output: `APPLICATION_STATUSES`/`MANUALLY_SELECTABLE_STATUSES` extendidos en `src/lib/application-status.ts`, `statusSchema` restringido en el Server Action manual, `STATUS_META`/`SelectContent` extendidos en `StatusDropdown`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-perfil-y-etapas-de-tracking/05-CONTEXT.md
@.planning/phases/05-perfil-y-etapas-de-tracking/05-UI-SPEC.md
@.planning/research/ARCHITECTURE.md
@DESIGN.md
@src/lib/application-status.ts
@src/app/actions/applications.ts
@src/components/dashboard/status-dropdown.tsx
@src/db/queries/applications.ts
@scripts/verify-a11y.ts
@scripts/verify-applications.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Extender APPLICATION_STATUSES + STATUS_META + restricción de selección manual</name>
  <files>src/lib/application-status.ts, src/app/actions/applications.ts, src/components/dashboard/status-dropdown.tsx, scripts/verify-status-extension.ts</files>
  <action>
    Extender APPLICATION_STATUSES en src/lib/application-status.ts con los 3 valores intermedios nuevos, en los nombres exactos snake_case locked por CONTEXT.md sección "Etapas intermedias de tracking" — auto_fill_in_progress, ready_to_review, submitted — agregados después de los 6 existentes. Agregar una segunda constante exportada MANUALLY_SELECTABLE_STATUSES con exactamente los 6 valores originales (saved, not_applied, applied, in_progress, rejected, accepted) — esta pasa a ser la única fuente de verdad de "lo que Juan puede elegir a mano", per la decisión explícita de CONTEXT.md de que las 3 etapas nuevas son de solo-lectura en el dropdown ("Juan no las selecciona a mano, solo el callback de Phase 6 las setea" — nota: ROADMAP.md's criterio de éxito #3 de Phase 5 dice que el dropdown "muestra y permite seleccionar" las etapas nuevas, pero la decisión más específica y posterior de CONTEXT.md — reforzada por la sección "Status Extension" completa de 05-UI-SPEC.md — las deja como solo-visualización; este plan honra CONTEXT.md/UI-SPEC por sobre la redacción más laxa de ROADMAP.md, satisfaciendo la mitad de "mostrar" de ese criterio de éxito sin hacerlas seleccionables a propósito). applications.status en sí no necesita ninguna migración — sigue siendo la misma columna text libre (research/ARCHITECTURE.md: "Keep it a free-text column; extend the Zod/TS constant, not the DB schema").

    En src/app/actions/applications.ts, cambiar statusSchema de z.enum(APPLICATION_STATUSES) a z.enum(MANUALLY_SELECTABLE_STATUSES) (importar la nueva constante junto a la existente) — esto es una aplicación real del lado servidor, no solo una omisión de UI: incluso un cliente modificado que POSTee auto_fill_in_progress/ready_to_review/submitted directo a updateApplicationStatus queda rechazado, extendiendo la mitigación Zod-enum ya existente de T-03-01.

    En src/components/dashboard/status-dropdown.tsx: agregar Hourglass, Eye, CircleCheck al import de lucide-react; extender STATUS_META (tipado Record&lt;ApplicationStatus, {...}&gt;, por lo que TypeScript no compila hasta que las 9 claves estén presentes — el mismo guardrail de build-time ya documentado en research/ARCHITECTURE.md, no una máquina nueva) con auto_fill_in_progress → label "Auto-fill en curso" / Icon Hourglass, ready_to_review → label "Listo para revisar" / Icon Eye, submitted → label "Enviado (auto-apply)" / Icon CircleCheck — los labels en español exactos y la elección de íconos locked por la tabla "Status Extension" de 05-UI-SPEC.md (confirmados ahí como visualmente distintos de los 6 íconos existentes: Hourglass vs. el Clock de in_progress, CircleCheck vs. el CheckCircle2 más bold de accepted). Cambiar el loop de opciones de SelectContent de APPLICATION_STATUSES.map(...) a MANUALLY_SELECTABLE_STATUSES.map(...) — este es el mecanismo real que mantiene los 3 estados nuevos fuera de la lista elegible, mientras SelectValue (sin cambios) sigue renderizando el estado actual vía STATUS_META[status], desacoplado de la lista de opciones. Agregar un fondo violeta-tint al SelectTrigger solo cuando el estado ACTUAL es uno de los 3 valores auto (un Set con los 3 strings auto, chequeado una vez por render): aplicar las clases bg-accent text-accent-foreground condicionalmente — las variables CSS --accent/--accent-foreground de globals.css ya valen #1e1930/#e7e5e1, el hex exacto de "Violet-Tint Surface" que especifica 05-UI-SPEC.md, así que no se introduce ningún token nuevo; los 6 estados manuales mantienen su fondo plano actual sin cambios.

    Crear scripts/verify-status-extension.ts (mismo patrón ad hoc tsx-contra-Postgres-real que scripts/verify-applications.ts): importar MANUALLY_SELECTABLE_STATUSES y APPLICATION_STATUSES desde lib/application-status.ts y confirmar con assert que MANUALLY_SELECTABLE_STATUSES tiene exactamente los 6 valores originales (ninguno de los 3 nuevos) y que APPLICATION_STATUSES tiene los 9; tomar un external_id real de opportunities (mismo patrón que verify-applications.ts) y llamar upsertApplicationStatus (capa de queries, ya genérica) con cada uno de los 3 valores nuevos en secuencia, confirmando vía getApplicationsByExternalIds que cada escritura se lee de vuelta correctamente — probando que la columna acepta los 3 valores nuevos a nivel de datos sin ninguna migración. Restaurar/limpiar la fila de test al final (mismo criterio pre-existente/cleanup que verify-applications.ts).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm build && DATABASE_URL=$DATABASE_URL pnpm exec tsx scripts/verify-status-extension.ts</automated>
  </verify>
  <done>pnpm build compila solo una vez STATUS_META cubre las 9 claves (guardrail de TypeScript); scripts/verify-status-extension.ts confirma que MANUALLY_SELECTABLE_STATUSES contiene exactamente los 6 valores originales, que APPLICATION_STATUSES contiene los 9, y que upsertApplicationStatus/getApplicationsByExternalIds (capa de datos) escriben y leen correctamente cualquiera de los 3 estados nuevos sobre un external_id real.</done>
</task>

<task type="auto">
  <name>Task 2: Verificación real en navegador — estados nuevos no seleccionables, tinte violeta visible</name>
  <files>scripts/verify-status-extension.ts</files>
  <action>
    Extender scripts/verify-status-extension.ts (Playwright, misma convención chromium.launch() + baseUrl = process.argv[2] ?? "http://localhost:3921" que scripts/verify-a11y.ts, corrido contra el dev server ya levantado con pnpm dev): antes de navegar, usar la capa de queries directamente (upsertApplicationStatus, ya importado por Task 1) para setear el status de una oportunidad real trackeada a submitted — esto simula lo que el callback de Phase 6 hará eventualmente, ya que nada en la UI de esta fase puede setear un estado auto (por diseño). Navegar al dashboard, ubicar el SelectTrigger de esa fila, y confirmar con assert: (a) el texto renderizado del trigger incluye "Enviado (auto-apply)" y su color de fondo computado resuelve al token accent (#1e1930) — reusar la técnica resolveToRgba basada en canvas de verify-a11y.ts si una comparación directa de string con getComputedStyle no es confiable contra la salida oklab de Tailwind v4 (mismo problema ya resuelto ahí); (b) abrir el SelectContent del trigger (teclado: Enter/Espacio, misma convención del recorrido de a11y ya existente) expone exactamente 6 elementos [role="option"], ninguno cuyo texto matchee "Enviado (auto-apply)"/"Auto-fill en curso"/"Listo para revisar" — probando que los 3 estados auto están genuinamente ausentes de la lista elegible, no solo visualmente atenuados. Restaurar el status original de la fila de test (o borrarla si no preexistía, mismo criterio de tracking/cleanup que verify-applications.ts) al final.
  </action>
  <verify>
    <automated>DATABASE_URL=$DATABASE_URL pnpm exec tsx scripts/verify-status-extension.ts http://localhost:3921</automated>
  </verify>
  <done>El script confirma en un navegador real: el trigger de una fila con estado auto-apply muestra ícono+texto+fondo violeta-tint correctos, y el SelectContent abierto contiene exactamente los 6 estados manuales — ninguno de los 3 nuevos aparece como opción.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client dropdown → updateApplicationStatus Server Action | Igual que T-03-01 (03-CONTEXT.md): el valor de status es no confiable; ahora también debe rechazar los 3 valores nuevos de solo-lectura, no solo validar que sea "alguno de los 9" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | updateApplicationStatus (app/actions/applications.ts) | medium | mitigate | statusSchema restringido a MANUALLY_SELECTABLE_STATUSES (6 valores) en vez de APPLICATION_STATUSES (9) — un cliente modificado que intente POSTear auto_fill_in_progress/ready_to_review/submitted a mano queda rechazado por Zod antes de tocar Postgres, extendiendo la mitigación ya existente de T-03-01 |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit && pnpm build` limpio
- `scripts/verify-status-extension.ts` pasa contra Postgres real + navegador real (dev server en :3921)
- Revisión manual: confirmar que StatusDropdown nunca ofrece los 3 estados nuevos como opción, y que un estado auto-apply preexistente se ve con ícono/texto/fondo distintivo
</verification>

<success_criteria>
El dropdown de estado de una postulación muestra las 3 etapas intermedias nuevas con ícono/texto/fondo propios cuando ya están seteadas en DB, pero Juan nunca puede seleccionarlas a mano — ni desde la UI ni vía el Server Action manual (defensa en profundidad) — satisfaciendo TRACK-05 (la columna acepta los nuevos valores sin migración) y TRACK-06 (la UI las refleja) tal como los precisa CONTEXT.md/UI-SPEC.
</success_criteria>

<output>
Create `.planning/phases/05-perfil-y-etapas-de-tracking/05-02-SUMMARY.md` when done
</output>
